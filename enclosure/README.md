# Bangla Braille Tutor — enclosure & fabrication

Mechanical files for the ESP32 Bangla Braille Tutor console. The top face is
the part that matters: it carries every component the learner touches.

## Three parts, one enclosure

The enclosure is three separate printed parts, stacked and screwed together:

1. **`top_panel.stl`** — the flat top panel. Already printed.
2. **`case_walls.stl`** — an open-top, open-bottom frame the panel rests
   on. **Print this next.**
3. **`case_base.stl`** — a flat cover that closes the bottom. **Print this
   too.**

`case_walls.stl` and `case_base.stl` are generated straight from
`tools/gen_case_parts_3d.py`, the same no-OpenSCAD-needed approach as
`top_panel.stl` -- `case_walls.scad` / `case_base.scad` are the OpenSCAD
source of truth for the same two parts, for anyone who does have OpenSCAD.

Stack order, bottom to top: the base cover, then the wall frame, then the
top panel on top. Four M3 screws drop down
through the panel's four corner holes, through the frame's open interior,
into the screw bosses moulded into the base cover.

Heights: 2.8 mm (panel) + 45.2 mm (walls) + 2.8 mm (base) = 50.8 mm (2 in)
total, ground to top panel. All three share the same 210 × 140 mm footprint
and 8 mm corner radius (from `top_panel_layout.scad`), so nothing needs
separate measuring — they line up by construction.

## Files

### The top panel, which is the part that gets made

| File | Purpose | Opens in |
|---|---|---|
| `top_panel.stl` | **The 3D top panel.** Watertight solid, mm, print or view it. **Generated.** | Cura, PrusaSlicer, Bambu Studio, any mesh viewer |
| `top_panel_3d.html` | **3D viewer for the top panel.** Orbit, preset views. **Generated.** | any browser (needs internet for the CDN) |
| `top_panel_cutout.svg` | 1:1 cut file for the same panel. **Generated.** | Laser cutter, Illustrator, CorelDRAW, Inkscape |
| `top_panel_layout.scad` | The same coordinates as OpenSCAD constants. **Generated.** | included by the case file |

### The rest

| File | Purpose |
|---|---|
| `case_walls.stl` | **The middle part.** Watertight solid, mm, ready for a slicer. An open-top/open-bottom frame, 45.2 mm tall, same footprint and corner radius as the top panel. No panel cutouts (motor/button/speaker) — those are already on the printed panel. Carries the wire pass-through hole, offset to one side of the back wall. **Generated.** |
| `case_base.stl` | **The base half.** Watertight solid, mm, ready for a slicer. Closes underneath: a 2.8 mm flat cover, same footprint and corner radius as the top panel, with M3 screw bosses under the panel's clearance holes. **Generated.** |
| `case_walls.scad` | OpenSCAD source of truth for `case_walls.stl`, for anyone who has OpenSCAD instead of Python |
| `case_base.scad` | OpenSCAD source of truth for `case_base.stl` |
| `braille_tutor_case.scad` | Superseded. An all-in-one alternative that merges the top panel and the walls into a single printed part — duplicates `top_panel.stl` rather than pairing with it. Use `case_walls.stl`/`.scad` instead unless you want a single-part top half printed from scratch |
| `3d_preview.html` | Superseded. An older mockup of the whole console with a layout that no longer matches anything. Nothing is made from it. |
| `case_full_3d.html` | 3D viewer: base cover only, side walls only, or the complete assembly (with the already-printed top panel shown for reference). **Generated by hand, not a script** -- built to the same numbers as the two `.scad` files |

Every generated file traces to one place. Re-run these after any change; never
hand-edit the outputs.

```
python tools/gen_top_panel.py       # SVG + SCAD layout, validates clearances
python tools/gen_top_panel_3d.py    # top_panel.stl + its HTML viewer
python tools/gen_case_parts_3d.py   # case_walls.stl + case_base.stl
```

[`tools/gen_top_panel.py`](../tools/gen_top_panel.py) holds the numbers.
[`tools/gen_top_panel_3d.py`](../tools/gen_top_panel_3d.py) and
[`tools/gen_case_parts_3d.py`](../tools/gen_case_parts_3d.py) import them, so
none of the three printed parts can drift out of alignment with each other.

### The STL

A flat plate, 210 x 140 x 2.8 mm, with all 54 holes. No walls, no skirt, no
base. It is built without a mesh library, so it is verified rather than
trusted:

| Check | Result |
|---|---|
| Triangles | 12,184 |
| Non-manifold edges | 0, watertight |
| Volume vs analytic | agrees to 1.7e-15 relative |
| Bounding box | 210.000 x 140.000 x 2.800 mm |
| Degenerate triangles | 0 |

Printed in PLA at 100% that is about 96 g of material. Print it flat on the
bed, holes vertical, no supports needed.

### The viewer renders the same mesh

`top_panel_3d.html` embeds the verified triangulation rather than rebuilding
the shape in the browser. `THREE.ExtrudeGeometry` is deliberately not used:
its hole bridging emits degenerate slivers, and `EdgesGeometry` then infers
edges along them, which drew a phantom line joining two neighbouring button
holes. The panel itself was solid there the whole time. Outlines are now drawn
straight from the ring loops, so nothing is inferred and the viewer cannot
disagree with the STL.

### Spacing between holes

Both grids share a 30 mm pitch, horizontally and vertically.

| Between | Pitch | Hole | Gap of material |
|---|---|---|---|
| Button to button, left-right | 30 mm | 12.4 mm | 17.6 mm |
| Button to button, top-down | 30 mm | 12.4 mm | 17.6 mm |
| Motor to motor, left-right | 30 mm | 10.4 mm | 19.6 mm |
| Motor to motor, top-down | 30 mm | 10.4 mm | 19.6 mm |

The submit button keeps the same 30 mm pitch below the last button row, so it
also has 17.6 mm of material above it.

The generator refuses to pass if any two cuts come within 2 mm of each other
or of the panel rim. Current worst cases: 6.40 mm of material from a corner
screw to the rim, and a 2.50 mm web between two adjacent grill holes.

## Parts the top face is cut for

| Component | Qty | Part size | Cutout |
|---|---|---|---|
| Tactile push button, 4-pin | 7 | 12.0 × 12.0 mm body, 6.0 mm tall | 12.4 mm square, 0.5 mm corner relief |
| Coin vibration motor | 6 | Ø 10.0 mm, 3.0 mm thick | Ø 10.4 mm |
| Speaker | 1 | Ø 40.0 mm | 37 × Ø 2.5 mm over a Ø 34.5 mm area |
| M3 machine screw | 4 | M3 | Ø 3.2 mm |

Button caps are white for dots 1–3, red for dots 4–6, and yellow for submit.

**Fit.** Every cutout is 0.4 mm larger than its part, 0.2 mm per side. Cut on
the path centreline and let the kerf give the running clearance; on a laser
that typically lands within a tenth of a millimetre.

Button bodies press **up** through their square holes, so the 2.8 mm panel
captures each switch just under its cap and 3.2 mm of body stands proud.
Motors bond into their round holes with the face **flush** to the panel top,
so the fingertip rests on the motor itself rather than on plastic over it.
Motor leads pass back down through the same hole.

## Layout

210 × 140 mm, 8 mm corner radius, 2.8 mm sheet.

Two 3 × 2 blocks share one 30 mm grid, with rows at y = 26, 56 and 86 mm.
Dots 1-2-3 run down the left column of each block and 4-5-6 down the right,
so dot N sits on the same line whether the learner is feeling it or pressing
it. The motor block centres on x = 52, the button block on x = 158, and the
submit button sits between the two button columns at x = 158, y = 116,
carrying the same 30 mm pitch down from the last row.

The speaker grill centres on x = 105, the exact panel centre line, at
y = 112. Its perforated area stops at Ø 34.5 mm so the Ø 40 mm speaker frame
seats against solid material.

Corner screws sit 8 mm in from both edges, on the corner-radius centres.

## The side walls

`case_walls.scad` is an open-top, open-bottom rounded-rectangle tube that the
already-printed top panel rests on. It shares `PANEL_W`, `PANEL_H` and
`CORNER_R` from `top_panel_layout.scad`, so its footprint and corner radius
match the panel exactly -- the panel sits flush on its top rim, no fitting.

Height: the brief is a total assembled height of 2 in (50.8 mm), ground to
top panel. The panel itself is 2.8 mm and the base cover (below) is another
2.8 mm, so the walls carry the rest: 50.8 - 2.8 - 2.8 = 45.2 mm.

It has none of the panel's own cutouts (motor, button, speaker) -- those are
already on the printed panel. The four M3 screws that pass down through the
panel's corner holes land inboard of the wall thickness, in the frame's open
interior, so the walls don't need their own screw holes either.

| Property | Value |
|---|---|
| Footprint | 210 × 140 mm, 8 mm corner radius (same as the top panel) |
| Height | 45.2 mm |
| Wall thickness | 2.8 mm, matching the top panel |
| Wire pass-through | Ø 8 mm round hole through the back wall, offset to one side (x = 40, not the wall's centre at x = 105) at z = 20 mm, for the power/USB cable |

- **Material:** PLA, PETG or ABS
- **Layer height:** 0.20 mm, or 0.16 mm for a smoother finish
- **Infill:** 20–30% gyroid or grid
- **Supports:** none needed; open top and bottom, no overhangs

`case_walls.stl` is built as 8 independent solids (4 corners, 4 straight
strips, one of them with the wire hole actually cut through it) concatenated
into one file -- they only ever touch a neighbour at a shared, zero-area
face, never overlap, so each is checked on its own:

| Check (each of the 8 pieces) | Result |
|---|---|
| Non-manifold edges | 0, watertight |
| Volume vs analytic | within 0.1%, the polygon-vs-circle/arc discretisation |
| Degenerate triangles | 0 |
| Total | 996 triangles, 85,596.6 mm3 |

## The base cover

`case_base.scad` closes underneath the walls. Same footprint and corner
radius as the top panel and the walls, so all three line up without fitting.

| Property | Value |
|---|---|
| Footprint | 210 × 140 mm, 8 mm corner radius (same as the top panel) |
| Thickness | 2.8 mm (flat cover) |
| Assembled height | 2.8 mm panel + 45.2 mm walls + 2.8 mm cover = 50.8 mm (2 in) total |
| Screw bosses | 4, Ø 7 mm OD × 10 mm tall, Ø 2.6 mm self-tapping pilot for M3, centred on the same `SCREW_POS` as the top panel's clearance holes; they rise into the walls' clear interior |

Flat and thin, it prints supportless, flat side down, same as the top panel.

- **Material / layer height / infill:** same as the walls, above.
- **Fasteners:** the same 4 × M3 screws that pass through the top panel and
  down through the walls thread into these bosses from underneath.

`case_base.stl` is the plate plus the 4 bosses, 5 independent solids
(touching, not overlapping) concatenated the same way as the walls:

| Check (each of the 5 pieces) | Result |
|---|---|
| Non-manifold edges | 0, watertight |
| Volume vs analytic | within 0.3% (the bosses' small radius makes their circle-discretisation error the largest of any part here) |
| Degenerate triangles | 0 |
| Total | 1,932 triangles, 83,489.0 mm3 |

## Known gap

`3d_preview.html` hardcodes its own copy of the geometry and still shows the
older layout: round button holes, a different grid, and a smaller grill. It is
a presentation mockup, not a fabrication source, so nothing is cut or printed
from it. Regenerate or retire it before using it to check dimensions.
