# Final Demo Presenter Checklist

Before presenting `EEE416_G8_FinalDemo.pptx` (30 slides), do the following.

## 1. Replace placeholders (all currently clean dashed-border boxes — this is intentional, not broken)

- **Hardware photos** — `[ Insert hardware photo here ]` on slides **11, 12, 13, 14**. Slide 14 needs three: angle 1, angle 2, in-use. Use real photos of the assembled breadboard/ESP32 build, not renders.
- **Demo video** — `[ Insert demo video link here ]` on slide **27** (Communication). Paste a real, working video URL (unlisted YouTube/Drive link is fine).
- **Logbook screenshot** — slide **26** also has an empty placeholder (`[ Insert logbook screenshot here ]`); grab a real screenshot of the project logbook before presenting.

## 2. Double-check before presenting

- Slide 1 said "$10 microcontroller" while the model diagram says the ESP32 itself is "~$4" and the BOM totals ~$10.25. Fixed to "$10 device" during this review — just confirm it reads correctly on your machine.
- Slide 19 (Limitations) and slide 24 (contributions table) are text-dense — rehearse pacing so you're not just reading bullets.
- Time the full 30-slide walkthrough once; slides 15–22 (Analysis section) are the densest stretch.
